# Xで検索 (X Search)

選択したテキストを右クリック→「Xで検索」で検索できるChrome拡張機能。

## インストール

1. Chromeで `chrome://extensions` を開く
2. 右上の **デベロッパーモード** をONにする
3. **「パッケージ化されていない拡張機能を読み込む」** をクリック
4. `x-search` フォルダを選択

## 使い方

検索したいテキストを選択 → 右クリック → **「Xで「〇〇」を検索」** をクリック → 新規タブで `x.com/search` が開く
